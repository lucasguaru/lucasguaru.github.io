# Ponte para product-definition

Este monorepo de desenvolvimento referencia o repositório de definição de produto.

```yaml
# Ver productDefinition em workspace-links.yaml
```

Mapping canônico: `../product-definition/mappings/mulesoft-integrations.yaml`

O arquivo legado `mulesoft-operation-dependencies.yaml` foi substituído pelo mapping em nível integração no product-definition.
